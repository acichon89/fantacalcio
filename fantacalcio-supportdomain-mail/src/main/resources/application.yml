server:
  port: 8060
spring:
  messages:
    basename: i18n/messages
    cache-seconds: -1
    encoding: UTF-8
    fallback-to-system-locale: true
  cloud:
    stream:
      bindings:
        confirmationMailSubscribeChannel:
          destination: confirmation-mail-queue